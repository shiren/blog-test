// import Home from '@/app/post/page/[page]/page';
//import generateRSSFeedXML from '../../rss';

// if (process.env.NODE_ENV === 'production') {
//   console.log('Generating RSS feed...');
//   generateRSSFeedXML();
// }

const Home = () => {
  return <div>heeel</div>;
};

export default Home;
